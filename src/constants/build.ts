/**
 * ビルドの識別子。デプロイ時に焼き込む。
 *
 * 「新しい機能が出ない」時に、端末が古いページを掴んでいるのか、
 * そもそも配信されていないのかを切り分けるために画面へ出す。
 * これが無いと、原因の特定に往復が必要になる(実際に起きた)。
 */
export const BUILD_ID = process.env.EXPO_PUBLIC_BUILD_ID ?? '開発中';
